//! Trunk: `InputEmbedder`, `MSAModule`, `PairformerModule`, template modules, recycling.
//!
//! Python: `boltz.model.modules.trunkv2` and related. Implementation is staged here.

/// Placeholder for the pairwise trunk stack (z/s tensors).
#[derive(Debug, Default)]
pub struct TrunkV2;

impl TrunkV2 {
    pub fn new() -> Self {
        Self
    }
}
